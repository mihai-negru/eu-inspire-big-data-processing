apiVersion: flink.apache.org/v1beta1
kind: FlinkDeployment
metadata:
  name: flink-inspire
  namespace: flink
spec:
  image: matrix22/flink-inspire:v2.2.0
  imagePullPolicy: Always
  flinkVersion: v1_20
  flinkConfiguration:
    taskmanager.numberOfTaskSlots: "1"
  serviceAccount: flink
  podTemplate:
    apiVersion: v1
    kind: Pod
    metadata:
      name: pod-template
    spec:
      serviceAccount: flink
      containers:
        - name: flink-main-container
          envFrom:
            - secretRef:
                name: flink-secret
  jobManager:
    resource:
      memory: "2048m"
      cpu: 1
  taskManager:
    resource:
      memory: "2048m"
      cpu: 1
  job:
    jarURI: local:///opt/flink/usrlib/flink-inspire-transformer.jar
    parallelism: 2
    upgradeMode: stateless
