#metrics:
#  jmx:
#    enabled: true

overrideConfiguration: |
#  # If replicationCount is set above >=3 then remove this
#  offsets.topic.replication.factor=1
#  transaction.state.log.replication.factor=1
  transaction.max.timeout.ms=900000

tls:
  enabled: false
  autoGenerated:
    enabled: false

listeners:
  client:
    protocol: PLAINTEXT
  controller:
    protocol: PLAINTEXT
  interbroker:
      protocol: PLAINTEXT
  external:
    protocol: PLAINTEXT

broker:
  overrideConfiguration: |
    transaction.max.timeout.ms=900000

controller:
  replicaCount: 3

  readinessProbe:
    initialDelaySeconds: 15
    periodSeconds: 30
    timeoutSeconds: 30
    failureThreshold: 10

  overrideConfiguration: |
    transaction.max.timeout.ms=900000

service:
  nodePorts:
    client: 30001

provisioning:
  enabled: true
  topics:
    - name: raw
      partitions: 1
      replicationFactor: 1
    - name: command
      partitions: 1
      replicationFactor: 1
    - name: validator.input
      partitions: 1
      replicationFactor: 1
    - name: validator.output
      partitions: 1
      replicationFactor: 1